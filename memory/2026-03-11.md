# 2026-03-11

## Durable notes (evening)

- Kai confirmed scaling direction: prioritize profitability via capacity optimization, but keep risk controls active.
- Hard rule reiterated: per-order max can be up to $100, but final size must still be risk-constrained.
- Clarified and implemented mode behavior:
  - **Live exposure refill mode** = enabled (daily turnover cap disabled).
  - `daily_new_open_notional_cap_usd = 0` means no daily turnover blocking; reopening depends on freed live exposure.
- Risk setting moved and enforced to:
  - `daily_stop_loss_usd = -50`
- Execution bottlenecks were opened up (with risk still present):
  - `trade_size_usd = 100`
  - `max_open_exposure_usd = 200`
  - `max_event_cluster_exposure_usd = 100`
  - `max_bet_fraction = 0.10`
  - compounding caps raised (`compound_trade_size_fraction=0.10`, `compound_max_open_exposure_fraction=0.20`, `compound_trade_size_max_usd=100`, `compound_max_open_exposure_min_usd=200`).
- New guardrail added per Kai request:
  - `min_open_size_usd = 1.0` (new tiny tickets like 0.26 are now filtered out).
  - Existing legacy <$1 positions are still in state unless explicitly cleaned.
- Known runtime issue to remember:
  - Gamma/requests SSL timeout spikes can fail a tick intermittently; monitor resumes on next cycle.

## Commits made

- `fb8e0a5` fix(weather-bot): add missing `tail_size_cap_fraction` default
- `04a4f5f` feat(weather-bot): enforce -50 daily stop and 250 daily new-open cap
- `9eb767a` feat(weather-bot): support live-exposure mode by disabling daily new-open cap
- `09493ec` feat(weather-bot): uncap sizing bottlenecks and enforce min open size

## Snapshot around 20:10

- Monitor running under new params.
- State moved quickly after cap changes (open positions and exposure increased); keep close watch on drawdown behavior after this rollout.
