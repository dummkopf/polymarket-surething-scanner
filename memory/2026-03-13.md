# 2026-03-13

## Durable notes (pre-compaction)

- Confirmed from Open-Meteo Terms (free non-commercial): **<10,000 API calls/day**, **<5,000/hour**, **<600/minute**.
- Live raw check for Shanghai:
  - Geocoding endpoint returned valid coordinates (`31.22222, 121.45806`).
  - Forecast endpoint returned: `{"error": true, "reason": "Daily API request limit exceeded. Please try again tomorrow."}`.
- Current weather-bot request budget estimate (from live pipeline shape):
  - ~63 Open-Meteo calls per run (city/date forecast + geocoding misses).
  - At current effective cycle (~205s run time + 180s sleep), ~224 runs/day.
  - Estimated ~14.1k calls/day, which exceeds the 10k/day limit.
- Frequency guidance prepared:
  - If only changing cadence (no architecture change): **10 min (600s) safer**, **12 min (720s) very safe**.
  - Theoretical floor to stay under 10k/day with current call volume is ~339s interval, but little safety margin.
- Open-Meteo docs indicate model refresh is **not a single fixed interval**; update frequency varies by model (examples seen in docs snippet: ICON ~3h, GFS/HRRR ~1h, ECMWF ~6h, UKMO ~1h, KMA ~6h).
- Pending close-loop user question: whether to switch monitor cadence to **30 min** now vs keep faster loop with weather-call caching/throttling split.
