# 2026-03-23

## Durable notes (pre-compaction)

- User directive (explicit): when odds/edge looks unbelievable, treat it as suspicious and **scrutinize logic first**; expected edge should be decent but not extreme.
- Implemented NO synthetic ask conservatism end-to-end in `paper_runner.py`:
  - `no_ask_penalty_amount(...) = max(no_synthetic_ask_penalty, yes_spread * no_synthetic_ask_spread_mult)` for `no_ask_synthetic_from_yes_bid`.
  - `apply_no_ask_penalty(...)` now takes `yes_spread` and uses the helper.
  - Call sites updated (signal build + position edge recompute path).
- Added auditable pricing/context fields (signal + position paths), including:
  - `resolution_station_code`, `forecast_coord_key`
  - `no_ask_raw`, `no_ask_penalty_applied`
  - entry-time traces (`entry_price_source`, `spread_at_entry`, `no_ask_raw_at_entry`, `no_ask_penalty_applied_at_entry`).
- Added extreme-edge sanity gate in `paper_runner.py`:
  - New config: `max_edge_sanity=0.35`, `max_effective_net_edge_sanity=0.30`.
  - Signals breaching threshold are blocked to watch-only with `edge_sanity_block=true` + reason.
  - Added scan counter: `edge_sanity_blocked`.
- Wired new knobs through runtime surfaces:
  - CLI args in `paper_runner.py` (`--max-edge-sanity`, `--max-effective-net-edge-sanity`).
  - Env + pass-through in `scripts/monitor_loop.sh`.
  - Service env in both workspace and user unit files (`systemd/polymarket-weather-monitor.service` and `~/.config/systemd/user/...`).
- Validation evidence snapshot (manual run-once at ~2026-03-22 22:14 CST):
  - `scan.total_slugs=1793`, `model_ready=421`, `signals=33`, `open_positions=11`.
  - Observed very large top edge example (`dallas ... 84forhigher`, edge ~0.6877), which motivated hard sanity blocking.
- Heartbeat bookkeeping update committed:
  - Commit `0a61edf` updates `memory/heartbeat-state.json` timestamp (`last_work_progress_sent_at=2026-03-22T22:14:00+08:00`).
- Pending follow-up after flush:
  - Latest code edits are still uncommitted (not yet finalized/pushed).
  - Need daemon restart + 2–3 tick stability check to confirm `rc=124` timeout risk is cleared.
