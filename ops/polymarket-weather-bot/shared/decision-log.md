# Shared Decision Log

## 2026-03-09
- Use two-agent model:
  - Agent A = Product/Strategy
  - Agent B = Code/Review
- Keep weather project in GitHub with env-safe publishing rules.
- Keep monitor running continuously for paper mode.
- Alert routing: use target chat_id (username-only routing is unreliable).
